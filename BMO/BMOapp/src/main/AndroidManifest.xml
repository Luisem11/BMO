<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.edu.udea.bmo">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".View.User.MainActivityUser" />
        <activity
            android:name=".View.Login.LoginActivity"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".View.Tutor.MainActivityTutor" />
        <activity android:name=".View.User.TutorDetailActivity" />
        <activity
            android:name=".View.User.ConfigActivity"
            android:label="@string/title_activity_config"
            android:theme="@style/AppTheme" />

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".View.User.MapsActivity"
            android:label="@string/title_activity_maps" />
        <activity android:name=".View.User.SubjectProgramActivity" />
        <activity android:name=".View.User.ChatActivity" />
        <activity android:name=".View.User.AboutActivity" />
        <activity android:name=".View.User.AllTutorsActivity" />
        <activity android:name=".View.User.SubjectFindActivity"></activity>
    </application>

</manifest>